- name: Install Zabbix
  hosts: all
  become: yes
  become_user: ansadmin
  become_method: sudo
  tasks:
    - name: Install prerequisites
      apt: pkg={{ item }} state=present update_cache=yes
      with_items:
        - wget
        - net-tools
      debug:
        msg: "Done!"
